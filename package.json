{
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.8",
    "prettier": "2.6.2"
  },
  "scripts": {
    "prepare": "husky install",
    "test": "prettier --check .",
    "dev": "nodemon backend_with_db.js",
    "start": "node backend_with_db.js"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "dependencies": {
    "ajv-formats": "^2.1.1"
  }
}
